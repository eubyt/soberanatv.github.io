import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { IconList, Icon } from ".";

<Meta title="Components/Icon" />

export const ListIcon = () => (
	<div>
		{Object.keys(IconList).map((icon) => (
			<div
				style={{
					margin: "10px",
					display: "flex",
				}}
			>
				<Icon name={icon} />
				<div
					className="text-xs"
					style={{
						margin: "0 10px",
						display: "flex",
						alignItems: "center",
					}}
				>
					{icon}
				</div>
			</div>
		))}
	</div>
);

With `MDX`, we can define a story for `Icon` right in the middle of our Markdown documentation.

<ListIcon />
