import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Link } from ".";

<Meta title="Components/Link" component={Link} />

export const Template = (args) => <Link {...args} />;

# Link

With `MDX`, we can define a story for `Link` right in the middle of our Markdown documentation.

<Canvas>
	<Story
		name="Link"
		args={{
			icon: "home",
			children: "Soberana",
		}}
	>
		{Template.bind({})}
	</Story>
</Canvas>
