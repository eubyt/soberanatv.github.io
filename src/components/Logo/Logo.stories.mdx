import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Logo } from ".";

<Meta title="Components/Logo" component={Logo} />

# Logo

With `MDX`, we can define a story for `Logo` right in the middle of our Markdown documentation.

<Canvas>
	<Story name="Logo">
		<Logo />
	</Story>
</Canvas>
