import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { Header } from ".";

<Meta title="Components/Header" component={Header} />

export const Template = (args) => (
	<div
		style={{
			width: "80vw",
		}}
	>
		<Header {...args}></Header>
	</div>
);

# Header

With `MDX`, we can define a story for `Header` right in the middle of our Markdown documentation.

<Canvas>
	<Story name="Header">{Template.bind({})}</Story>
</Canvas>
